name: Destroy Azure VNet with Terraform for dev environment

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to destroy (e.g., dev, prod)'
        required: true
        default: 'dev'
      auto_approve:
        description: 'Auto-approve destroy'
        required: true
        default: 'true'

jobs:
  terraform-destroy:
    name: Terraform Destroy
    runs-on: ubuntu-latest

    env:
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init -backend-config="../backend-bootstrap/backend-dev.tfvars"
        working-directory: ./terraform/vNET/dev

      - name: Terraform Validate
        run: terraform validate
        working-directory: ./terraform/vNET/dev

      - name: Terraform Plan (Destroy)
        run: terraform plan -destroy -var-file="../env/${{ github.event.inputs.environment }}.tfvars" -out=tfplan
        working-directory: ./terraform/vNET/dev

      - name: Terraform Destroy
        run: |
          if [ "${{ github.event.inputs.auto_approve }}" == "true" ]; then
            terraform destroy -auto-approve -var-file="../env/${{ github.event.inputs.environment }}.tfvars"
          else
            terraform destroy -var-file="../env/${{ github.event.inputs.environment }}.tfvars"
          fi
        working-directory: ./terraform/vNET/dev
